import React from 'react'
import './Aside.less'
const EsLayoutAside = (props: { children: React.ReactNode }) => {
  return <div className="es-layout-aside">{props.children}</div>
}

export default EsLayoutAside
